# myproxy

macOS GPUI controller for a bundled mihomo core. Canonical rules: `~/project/vibe/AGENTS.md`.

Demo UI lives in `src/`. Mock data only until the strategy document and core supervisor exist.
