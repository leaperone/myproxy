# 任务计划：app-design-fixes

- 任务 ID：`app-design-fixes-2026-09-06_23-19-37`
- 创建时间：`2026-09-06_23-19-37`

## 目标
基于最新 `origin/main` 修复 app design review 中确认的问题。

## 范围
- 修复窄窗口布局、长文本收缩和连接表可见性。
- 为主导航、卡片编辑、设置开关和动态状态补齐键盘/可访问语义。
- 为危险操作增加确认或明确反馈；统一中文文案。

## 非目标
- 不改变 mihomo 路由、策略 schema 或网络行为。
- 不引入新的 UI 框架或大规模视觉重构。

## 关键约束
- 遵守仓库 AGENTS.md；保持 GPUI/gpui-kit 现有架构。
- 不写时间或工作量估算。

## 修改路径
- `src/ui.rs`：页面布局、控件语义、反馈与文案。

## 验证方式
- `cargo check --quiet`。
- `cargo test --quiet`。
- `git diff --check`。
- macOS 上 Tab/Enter/Escape、VoiceOver 和删除确认流程现场复核。

## 验收标准
- 最小支持窗口下主要内容不被裁切，连接表可访问全部列或明确隐藏次要列。
- 主导航、节点组/规则编辑可通过键盘完成。
- 开关状态、动态状态、表单字段可被用户和辅助技术理解。
- 删除/批量关闭/互斥模式切换有明确风险提示。

## 未确认事项
- 当前容器未运行 macOS GUI，因此 VoiceOver 和现场窗口验收仍需在 macOS 完成。

## 执行状态
- [x] 完成只读探索并确认真实调用链
- [x] 完成实现
- [x] 完成验证
- [x] 完成交付前收敛检查

## 决策
| 决策 | 理由 |
|---|---|
| 使用任务 worktree 基于 `origin/main` 开发 | 当前主 checkout 落后远端，避免直接修改主分支 |
| 使用现有 Button/Toggle/Switch/Dialog | 复用 gpui-kit 的键盘、焦点和确认行为 |
| 连接列表保留固定诊断列并启用双向滚动 | 避免窄窗口裁切关键信息 |

## 错误与处理
| 错误 | 尝试 | 处理结果 |
|---|---:|---|
| 自定义导航首次编译缺少 `Collapsible` 导入 | 1 | 已补充导入 |
| 规则卡片按钮闭包括号错误 | 1 | 已重排并通过编译 |
| h_flex 不支持 role builder | 1 | 已移除行级 role，保留列表级语义 |
