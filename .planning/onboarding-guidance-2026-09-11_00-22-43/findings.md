# Findings

- 首屏 CTA 在 `src/ui.rs` overview，默认「连接」；状态句「在总览连接」。
- `onboard::should_prompt` 在 `AppView::new` 里对已签名 `.app` 弹 CLI。
- 规则只在 Mixed / 系统接管 / TUN 生效的合同只在 README / AGENTS。
- 系统设置完整路径只在 `onboard.rs`，设置页没有打开系统设置。
- `RoutingProfile::Allowlist` 按钮是「正面清单」，说明已写「未命中直连」。
- 连接页开关「显示核心直连」，筛空文案写「显示直连」。
