# 进度

## 当前增量

安装 GPUI 应用菜单：About、Check for Updates、Quit。

## 已完成

- 确认根因：未调用 `set_menus`。
- 已建 worktree `feat/app-menu-about-updates`。

## 进行中

- preflight / squash merge。

## 已验证

- `cargo check --bin myproxy` 通过（既有 unused warning，与本次无关）。
